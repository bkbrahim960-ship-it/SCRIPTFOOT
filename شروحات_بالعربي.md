

# 🅰️ شرح النشر على Render

تخيل أن Render **كومبيوتر بعيد** تشغل عليه الكود بتاعك. نضغط على الأزرار مع بعض.

## خطوة 1: ادخل Render.com

افتح متصفحك (Google Chrome) واكتب:
```
https://render.com
```

## خطوة 2: سجل الدخول

- اضغط على **Get Started** (فوق على اليمين)
- اضغط على **Continue with GitHub**
- حيفتحلك صفحة GitHub → اضغط **Authorize render**

الآن أنت داخل Render.

---

## خطوة 3: أنشئ Web Service

في صفحة Render الرئيسية:

- اضغط على **New +** (فوق على اليمين)
- راح تظهر قائمة → اختر **Web Service**

---

## خطوة 4: اربط GitHub

- راح تظهرلك قائمة بالمشاريع اللي عندك على GitHub
- ابحث عن **SCRIPTFOOT**
- اضغط على **Connect**

---

## خطوة 5: املأ الإعدادات

راح تظهر لك صفحة فيها حقين فارغين. املأهم بالضبط كالتالي:

### الحقل 1: Name
- اكتب: `scriptfoot`

### الحقل 2: Region
- اختر: `Frankfurt (EU)` ← ألمانيا (الأقرب للعالم العربي)

### الحقل 3: Branch
- اتركها: `master`

### الحقل 4: Runtime
- اختر: `Python 3`

---

### الحقل 5: Build Command (الأهم ⭐)

انزع الكلام اللي موجود وانسخ هذا بداله:
```
pip install -r requirements.txt && playwright install chromium
```

> هذا الأمر يثبت كل الأدوات اللي يحتاجها السكريبت

### الحقل 6: Start Command

اكتب هذا:
```
uvicorn api:app --host 0.0.0.0 --port $PORT
```

> هذا الأمر يشتغل السيرفر

### الحقل 7: Plan Type

- اختر **Free** ✅ (لأنه مجاني)

---

## خطوة 6: أضف متغير البيئة

تحت الشروط، راح تلاقي كلمة **Advanced** → اضغط عليها.

بعدين اضغط **Add Environment Variable** واملأ:

| Key | Value |
|-----|-------|
| `UPDATE_INTERVAL` | `60` |

بعدين اضغط **Add** (في نفس المكان)

---

## خطوة 7: انشر 🚀

- اضغط على الزر الأزرق: **Create Web Service**

🎉 الآن Render بدأ يشتغل. خلاص.

سترى شاشة سوداء وكتابات بيضاء تتحرك (هذا طبيعي). انتظر ... 

🟢 **لما تشوف كلمة LIVE باللون الأخضر → سيرفرك اشتغل ✅**

---

## خطوة 8: اختبر السيرفر

افتح علامة تبويب جديدة في المتصفح واكتب:
```
https://scriptfoot.onrender.com/api/health
```

إذا شفت رسالة مثل:
```json
{"status":"ok","matches_count":2,"live_count":1}
```

✅ **مبروك! السيرفر شغال**

جرب كمان:
```
https://scriptfoot.onrender.com/api/matches
```
راح تشوف المباريات.

---

## خطوة 9: ابقِ السيرفر شغال (مهم)

مشكلة Render المجاني: **يوقف السيرفر إذا ما استخدمته 15 دقيقة**.

الحل: نستخدم موقع اسمه **UptimeRobot** يرسل إشارة كل 5 دقائق عشان يبقى شغال.

1. افتح: https://uptimerobot.com
2. اضغط **Sign Up Free** → سجل بايميلك وكلمة سر
3. راح توصلك رسالة تأكيد → افتحها واضغط **Confirm**
4. في الصفحة الرئيسية، اضغط **Add New Monitor**
5. املأ:

| الحقل | اكتب هذا |
|-------|----------|
| Monitor Type | HTTP(s) |
| Friendly Name | scriptfoot |
| URL (or IP) | `https://scriptfoot.onrender.com/api/health` |
| Monitoring Interval | 5 minutes |

6. اضغط **Create Monitor** ✅

من الآن وصاعداً، السيرفر **شغال 24 ساعة بلا توقف**.

---

# 🅱️ شرح ربط السكريبت بموقعك

API معناه: **السيرفر يعطيك المباريات على شكل بيانات (JSON)** وموقعك يستقبلها ويعرضها.

## رابط السيرفر بتاعك هو:

```
https://scriptfoot.onrender.com
```

## روابط API المهمة:

| الرابط | ماذا يرجع؟ |
|--------|-----------|
| `https://scriptfoot.onrender.com/api/matches` | كل المباريات |
| `https://scriptfoot.onrender.com/api/matches/live` | المباريات المباشرة فقط |
| `https://scriptfoot.onrender.com/api/health` | صحة السيرفر |

---

### إذا موقعك React.js:

في أي ملف (مثلاً `MatchesPage.jsx`):

```jsx
import { useState, useEffect } from 'react';

function MatchesPage() {
  const [matches, setMatches] = useState([]);

  useEffect(() => {
    fetch('https://scriptfoot.onrender.com/api/matches/live')
      .then(res => res.json())
      .then(data => setMatches(data.data));
  }, []);

  return (
    <div>
      <h1>المباريات المباشرة ⚽</h1>
      {matches.map(match => (
        <div style={{border:'1px solid #ddd', padding:15, margin:10}}>
          <h3>{match.title}</h3>
          {match.league && <p>🏆 {match.league}</p>}
          {match.score && <p>⚽ {match.score}</p>}
          <p>🕒 {match.match_time}</p>
          {match.channels?.map(ch => <span>📺 {ch} </span>)}
          <br/>
          {match.streams?.map(s => (
            <a href={s.url} target="_blank" style={{display:'inline-block',background:'green',color:'white',padding:'8px 16px',margin:5,textDecoration:'none'}}>
              ▶ {s.quality || 'بث مباشر'}
            </a>
          ))}
        </div>
      ))}
    </div>
  );
}

export default MatchesPage;
```

---

### إذا موقعك HTML فقط:

حط هذا الكود في صفحة HTML:

```html
<div id="matches"></div>

<script>
fetch('https://scriptfoot.onrender.com/api/matches')
  .then(r => r.json())
  .then(d => {
    let html = '';
    d.data.forEach(m => {
      html += `<div style="border:1px solid #ddd; margin:10px; padding:10px">
        <h3>${m.title}</h3>
        ${m.league ? `<p>🏆 ${m.league}</p>` : ''}
        ${m.score ? `<p>⚽ ${m.score}</p>` : ''}
        ${m.streams.map(s => `<a href="${s.url}" target="_blank" style="background:green;color:white;padding:8px;margin:3px;display:inline-block">▶ بث مباشر</a>`).join('')}
      </div>`;
    });
    document.getElementById('matches').innerHTML = html;
  });
</script>
```

نسخ → لصق → تشتغل.

---

# 🆘 لو صارت مشكلة

| المشكلة | الحل |
|---------|------|
| Render يطلع خطأ Build Failed | تأكد من Build Command مكتوب بالضبط |
| السيرفر شغال بس ما في مباريات | انتظر دقيقة أو دقيقتين (التحديث كل 60 ثانية) |
| UptimeRobot ما يشتغل | تأكد من الرابط: `https://scriptfoot.onrender.com/api/health` |
| موقعي ما يجيب بيانات | افتح الرابط في المتصفح وشوف إذا يرجع بيانات. إذا إيوه → المشكلة في كود موقعك. إذا لا → المشكلة في السيرفر |

---

**✨ ألف مبروك. السكريبت الآن شغال وموقعك يجيب المباريات تلقائياً كل دقيقة.**
