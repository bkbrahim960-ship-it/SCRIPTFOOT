# الدليل الكامل - خطوة بخطوة للمبتدئين

## ما هذا المشروع؟

هذا سكريبت **يسحب المباريات وروابط البث المباشر** من مواقع عربية (يلا شوت، يلا كورة، أكوام، مشاهد، وغيرها) ويعرضها لك على هيئة **API** تقدر تربطه بموقعك.

يعني بدل ما تدور على روابط المباريات كل يوم، السكريبت **يشتغل لوحده** ويحدث البيانات كل دقيقة.

---

# الخطوة 1: إنشاء حساب GitHub

> GitHub هو موقع يستضيف الكود مجاناً

1. افتح المتصفح واذهب إلى: https://github.com/signup
2. اكتب **بريدك الإلكتروني** → Continue
3. أنشئ **كلمة سر** → Continue
4. اختر **اسم مستخدم** (مثلاً: `madjid123`) → Continue
5. أدخل الكود الذي وصلك في البريد
6. اختر **Free** (مجاني) → Continue
7. تأكد من تفعيل البريد الإلكتروني

---

# الخطوة 2: إنشاء مستودع (Repository) على GitHub

> المستودع هو مكان الكود على الإنترنت

1. في GitHub، اضغط على علامة **+** في أعلى اليمين → **New repository**
2. في حقل **Repository name**: اكتب `scriptmatch`
3. تأكد أن **Public** محدد
4. لا تضغط على أي شيء آخر
5. اضغط **Create repository**

⭐ **لا تغلق الصفحة!** ستحتاجها في الخطوة التالية.

---

# الخطوة 3: رفع الكود من جهازك إلى GitHub

الآن سنرفع مجلد `SCRIPTMATCH` من سطح المكتب إلى GitHub.

### أولاً: فتح PowerShell في المجلد الصحيح

1. افتح المجلد `SCRIPTMATCH` على سطح المكتب
2. في شريط العناوين (مكان المسار)، اكتب `powershell` واضغط Enter
   - ستفتح نافذة زرقاء (PowerShell) والمجلد مفتوح فيها

### ثانياً: تشغيل الأوامر

انسخ كل أمر والصقه في PowerShell واضغط Enter بالترتيب:

**الأمر 1:**
```
git init
```
> يبدأ مستودع محلي

**الأمر 2:**
```
git add .
```
> يضيف جميع الملفات

**الأمر 3:**
```
git commit -m "First commit"
```
> يحفظ نسخة أولى

**الأمر 4** (انسخ من صفحة GitHub):
```
git remote add origin https://github.com/your-username/scriptmatch.git
```
> ⚠️ استبدل `your-username` باسم المستخدم الذي اخترته.
> هذا الرابط ستجده في صفحة GitHub الرمادية بعد إنشاء المستودع.

**الأمر 5:**
```
git push -u origin main
```
> يرفع الكود إلى GitHub. قد يطلب منك:
> - **Sign in with browser** → اضغط وادخل حساب GitHub
> - انتظر حتى تنتهي العملية

✅ **تم!** ارجع إلى صفحة GitHub وحدثها (F5)، ستجد الملفات كلها مرفوعة.

---

# الخطوة 4: إنشاء حساب على Render

> Render هو سيرفر مجاني يشغل الكود الخاص بك

1. اذهب إلى: https://render.com
2. اضغط **Get Started** (أعلى اليمين)
3. اضغط **Continue with GitHub**
4. سيطلب صلاحية: اضغط **Authorize render**
5. أكمل التسجيل

---

# الخطوة 5: نشر المشروع على Render

### الجزء أ: ربط GitHub

1. في لوحة Render، اضغط **New +** (أعلى اليمين)
2. اختر **Web Service**
3. اضغط على **Connect account** → اختر **GitHub**
4. ابحث عن `scriptmatch` واضغط **Connect**

### الجزء ب: إعدادات النشر

املأ الحقول التالية بالضبط:

| الحقل | القيمة |
|-------|--------|
| **Name** | `scriptmatch` |
| **Region** | `Frankfurt (EU)` (الأقرب لك) |
| **Branch** | `main` |
| **Runtime** | `Python` |
| **Build Command** | يكتب التالي (انسخه): |

```
pip install -r requirements.txt && playwright install chromium
```

> هذا الأمر يُثبّت المكتبات ومتصفح Chromium

| **Start Command** | يكتب التالي: |
|---|---|
| `uvicorn api:app --host 0.0.0.0 --port $PORT` |

| **Plan** | اختر **Free** ✅ |
|---|---|

### الجزء ج: إضافة متغيرات البيئة

تحت الحقول، اضغط **Advanced** ثم **Add Environment Variable**:

- **Key**: `UPDATE_INTERVAL`
- **Value**: `60`
- اضغط **Add**

### الجزء د: النشر

اضغط **Create Web Service** ✅

⏳ **انتظر 5-10 دقائق** لأول مرة (يحمّل Chromium). سترى في الأسفل كتابات بيضاء تتحرك — هذا طبيعي.

🟢 **لما يصير الزر أخضر "Live"** → سيرفرك شغال.

انسخ الرابط (مثلاً: `https://scriptmatch.onrender.com`)

---

# الخطوة 6: اختبار السيرفر

افتح المتصفح وأدخل الرابط التالي (استخدم رابطك):

```
https://scriptmatch.onrender.com/api/health
```

إذا شفت رسالة مثل:
```json
{"status":"ok","matches_count":5,"live_count":2}
```

✅ **مبروك! السيرفر شغال والمباريات بدأت تجلب!**

جرب كمان:
```
https://scriptmatch.onrender.com/api/matches
```
لتشوف كل المباريات.

---

# الخطوة 7: منع السيرفر من التوقف (مهم جداً)

> Render المجاني يوقف السيرفر إذا ما استخدمته لمدة 15 دقيقة. الحل: نستخدم خدمة مجانية ترسل ping كل 5 دقائق لتبقيه شغال.

1. اذهب إلى: https://uptimerobot.com
2. اضغط **Sign Up Free** → سجل بحساب
3. من لوحة التحكم، اضغط **Add New Monitor**
4. املأ:

| الحقل | القيمة |
|-------|--------|
| **Monitor Type** | `HTTP(s)` |
| **Friendly Name** | `scriptmatch` |
| **URL (or IP)** | `https://scriptmatch.onrender.com/api/health` |
| **Monitoring Interval** | `5 minutes` |

5. اضغط **Create Monitor** ✅

🟢 بهذا السيرفر **ما يتوقف أبداً**.

---

# الخطوة 8: ربط السيرفر بموقعك

### لموقع React / Next.js / Vue.js

في مشروعك (مثلاً في أي ملف component):

```jsx
import { useState, useEffect } from "react";

export default function MatchesPage() {
  const [matches, setMatches] = useState([]);

  useEffect(() => {
    // جلب المباريات عند تحميل الصفحة
    fetch("https://scriptmatch.onrender.com/api/matches/live")
      .then(res => res.json())
      .then(data => setMatches(data.data));
  }, []);

  return (
    <div>
      <h1>المباريات المباشرة</h1>
      {matches.map(match => (
        <div key={match.id}>
          <h3>{match.title}</h3>
          {match.score && <p>النتيجة: {match.score}</p>}
          {match.streams.map((s, i) => (
            <a key={i} href={s.url} target="_blank">
              شاهد البث
            </a>
          ))}
        </div>
      ))}
    </div>
  );
}
```

### لموقع HTML عادي (بدون React)

```html
<div id="matches"></div>
<script>
fetch("https://scriptmatch.onrender.com/api/matches")
  .then(r => r.json())
  .then(data => {
    document.getElementById("matches").innerHTML = data.data
      .map(m => `<div><h3>${m.title}</h3></div>`).join("");
  });
</script>
```

---

# أوامر API متاحة (لربطها بموقعك)

| الرابط | الوصف |
|--------|-------|
| `GET /api/matches` | جميع المباريات |
| `GET /api/matches/live` | المباشرة فقط |
| `GET /api/matches?status=finished` | المنتهية فقط |
| `GET /api/matches?league=دوري+أبطال+أوروبا` | فلترة حسب الدوري |
| `GET /api/matches?source=yalla_shoot` | فلترة حسب المصدر |
| `GET /api/matches/{id}` | مباراة محددة برابطها |
| `GET /api/sources` | المصادر النشطة |
| `GET /api/health` | حالة السيرفر |

---

# مشاكل وحلول

**❌ السيرفر ما يشتغل (Build failed)**
- تأكد أن حقل **Build Command** مكتوب بالضبط:
  `pip install -r requirements.txt && playwright install chromium`
- اذهب إلى Render → Logs → شوف الخطأ

**❌ ما في مباريات تظهر**
- انتظر دقيقتين (التحديث يجري كل 60 ثانية)
- جرب: `https://scriptmatch.onrender.com/api/health`
- إذا `sources_active` فاضي → المواقع يمكن مسدودة من منطقة السيرفر

**❌ مشكلة في رفع الكود**
- تأكد أنك في مجلد `SCRIPTMATCH` الصحيح (الأمر `pwd` يظهر المسار)
- اكتب `git status` لترى الملفات الموجودة

---

# نصيحة أخيرة

إذا ما ظهرت مباريات بعد 5 دقائق، راسلني وراح أضبطها معاك. الدنيا سهلة إن شاء الله ✅
