# خطوات تشغيل السكريبت - مختصرة

## الكود مرفوع على GitHub → https://github.com/bkbrahim960-ship-it/SCRIPTFOOT

---

## ❶ أنشئ حساب على Render

1. افتح https://render.com
2. اضغط **Get Started** (فوق اليمين)
3. اضغط **Continue with GitHub**
4. سجل الدخول بحساب GitHub → **Authorize**

---

## ❷ انشر المشروع

1. من لوحة Render → **New +** → **Web Service**
2. اضغط على **Connect a GitHub repo**
3. ابحث عن `SCRIPTFOOT` → اضغط **Connect**

### املأ الحقول كالتالي:

| الخانة | اكتب هذا |
|--------|----------|
| **Name** | `scriptfoot` |
| **Region** | `Frankfurt (EU)` |
| **Branch** | `master` |
| **Runtime** | `Python 3` |
| **Build Command** | انسخ هذا بالضبط: |
| | `pip install -r requirements.txt && playwright install chromium` |
| **Start Command** | `uvicorn api:app --host 0.0.0.0 --port $PORT` |
| **Plan Type** | **Free** ✅ |

### أضف متغير بيئة:

- اضغط **Advanced** → **Add Environment Variable**
- **Key**: `UPDATE_INTERVAL`
- **Value**: `60`
- اضغط **Add**

### انشر:

- اضغط **Create Web Service** ✅

⏳ انتظر 5-10 دقائق حتى يشتغل (يظهر **Live** بالأخضر).

---

## ❷ اختبر السيرفر

افتح المتصفح وادخل:

```
https://scriptfoot.onrender.com/api/health
```

إذا شفت رسالة فيها `"status": "ok"` → **تمام**.

جرب كمان:

```
https://scriptfoot.onrender.com/api/matches
```

---

## ❸ ابقِ السيرفر شغال (UptimeRobot)

1. افتح https://uptimerobot.com → **Sign Up Free**
2. **Add New Monitor**
3. املأ:

| الخانة | القيمة |
|--------|--------|
| Monitor Type | `HTTP(s)` |
| Friendly Name | `scriptfoot` |
| URL | `https://scriptfoot.onrender.com/api/health` |
| Interval | `5 minutes` |

4. **Create Monitor** ✅

---

## ❹ اربطه بموقعك

في مشروعك React/Vue/Next.js استخدم:

```jsx
useEffect(() => {
  fetch("https://scriptfoot.onrender.com/api/matches/live")
    .then(r => r.json())
    .then(d => setMatches(d.data));
}, []);
```

---

### جميع روابط API

| المسار | الوصف |
|--------|-------|
| `/api/matches` | كل المباريات |
| `/api/matches/live` | المباشرة فقط |
| `/api/matches?league=دوري+أبطال+أوروبا` | فلترة |
| `/api/health` | حالة السيرفر |

---

**✨ تم. السكريبت شغال وبيحدث نفسه كل 60 ثانية.**
